1. To run the python program, you need to open terminal in this folder to execute it.
2. To run the python file you need to download numpy package.
2. To run the import_data.sql, you need to change the path of the excel files in import_data.sql
3. It may take around 10 min to execute the sql files of step_1, step_2 and step_3.